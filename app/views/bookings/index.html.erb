<div class="container mt-5">
  <h1>My Bookings</h1>
  <% @bookings.each do |booking| %>
    <ol>
      <li>
        <%= link_to booking.flat.name, booking_path(booking) %>
      </li>
    </ol>
  <% end %>
</div>
