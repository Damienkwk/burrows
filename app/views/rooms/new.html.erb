<div class="container">
  <div class="row">
    <div class="col"></div>
    <div class="col-6">
      <div class="rooms">
        <div class="add-room">
          <h1><%= @flat.name %></h1>
          <h3>Sleeping arrangements</h3>
          <p>Room <%= @flat.rooms.length + 1 %></p>
          <%= simple_form_for ([@flat, @room]) do |f| %>
            <%= f.input :name, label: "Type", as: :radio_buttons, collection: ["Bedroom", "Living Room"] %>
            <%= f.button :submit, class: "btn-ghost"%>
          <% end %>
          <br>
        </div>
      </div>
    </div>
    <div class="col"></div>
  </div>
</div>
